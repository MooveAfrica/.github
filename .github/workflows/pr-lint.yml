name: PR Description Check
on:
  pull_request:
    types: [opened, edited]
jobs:
  pr-description-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Check PR Description
        run: |
          if echo "${{ github.event.pull_request.body }}" | awk 'BEGIN { RS = "" } /## Description[[:space:]]+(.*)[[:space:]]+## Jira Links/ {
              match($0, /## Description[[:space:]]+(.*)[[:space:]]+## Jira Links/, arr);
              gsub(/^[[:space:]]+|[[:space:]]+$/, "", arr[1]);
              if (length(arr[1]) < 15) {
                  exit 0;
              }
              exit 1;
          }'; then
            echo "PR description: ${{github.event.pull_request.body}}"
            echo "Result: PR description length should be more t han 15 characters"
            exit 1
          fi
          if ! echo "${{ github.event.pull_request.title }}" | grep -E '[A-Z]+-[0-9]+'; then
            echo "PR title does not contain a Jira title in the format: ABCD-1234"
            exit 1
          fi
          if ! echo "${{ github.event.pull_request.body }}" | grep -Eq '\[\s*x\s*\]'; then
            echo "Please tick at least 1 checkbox in the checklist"
            exit 1
          fi
          if ! echo "${{ github.event.pull_request.body }}" | grep -E 'https?:\/\/(.*).atlassian.net\/browse\/[A-Z]+-[0-9]+'; then
            echo "PR description does not contain a Jira HTTP link"
            exit 1
          fi
          if ! echo "${{ github.event.pull_request.body }}" | grep -Eq '## Changes Made'; then
            echo "PR description does not contain a 'Changes Made' heading"
            exit 1
          fi
